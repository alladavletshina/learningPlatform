<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.9.xsd">

    <changeSet id="enum-1-create-user-role-type" author="your_name">
        <comment>Create user_role enum type</comment>
        <sql><![CDATA[
            DO $$ BEGIN
            CREATE TYPE user_role AS ENUM ('STUDENT', 'TEACHER', 'ADMIN');
            EXCEPTION
                WHEN duplicate_object THEN null;
            END $$;
            ]]></sql>
    </changeSet>

    <changeSet id="enum-2-create-enrollment-status-type" author="your_name">
        <comment>Create enrollment_status enum type</comment>
        <sql><![CDATA[
            DO $$ BEGIN
            CREATE TYPE enrollment_status AS ENUM ('ACTIVE', 'COMPLETED', 'CANCELLED', 'SUSPENDED');
            EXCEPTION
                WHEN duplicate_object THEN null;
            END $$;
            ]]></sql>
    </changeSet>

    <changeSet id="enum-3-create-question-type" author="your_name">
        <comment>Create question_type enum type</comment>
        <sql><![CDATA[
            DO $$ BEGIN
            CREATE TYPE question_type AS ENUM ('SINGLE_CHOICE', 'MULTIPLE_CHOICE', 'TRUE_FALSE', 'TEXT');
            EXCEPTION
                WHEN duplicate_object THEN null;
            END $$;
            ]]></sql>
    </changeSet>

</databaseChangeLog>